# Redis缓存

## 知识点解析

### 概述

Redis 是内存型键值数据库，常用于缓存、计数器、排行榜、分布式锁、会话存储和简单消息队列。面试中常问 Redis 为什么快、有哪些数据结构、持久化方式、高可用方案和缓存一致性问题。

### Redis 为什么快

Redis 快的主要原因：

- 数据主要在内存中，访问速度快。
- 单线程执行命令，避免大量锁竞争和上下文切换。
- 使用 IO 多路复用处理大量连接。
- 数据结构实现高效，例如 SDS、跳表、哈希表、压缩列表等。

需要注意，现代 Redis 并不是所有工作都只有一个线程，网络 IO、持久化和删除等任务可能使用后台线程，但命令执行主路径仍然强调单线程模型。

### 数据结构

| 数据结构 | 典型场景 |
| --- | --- |
| String | 缓存、计数器、分布式锁 |
| Hash | 用户对象、商品属性 |
| List | 简单队列、时间线 |
| Set | 去重、共同好友、标签 |
| ZSet | 排行榜、延迟队列 |
| Bitmap | 签到、布尔状态统计 |
| HyperLogLog | UV 近似统计 |

### 持久化

RDB 是快照持久化，按时间点生成数据快照，恢复快，但可能丢失最近一段时间数据。AOF 是追加写命令日志，数据更完整，但文件更大，恢复可能更慢。实际工程中可以组合使用。

### 高可用

Redis 高可用常见方案：

- 主从复制：读扩展和数据备份。
- 哨兵：监控主节点，故障时自动选主。
- Cluster：分片存储，支持横向扩展。

### 缓存一致性

常见做法是先更新数据库，再删除缓存。删除缓存而不是更新缓存，是为了避免并发写入时旧值覆盖新值。极端场景下仍可能短暂不一致，可以结合延迟双删、消息队列、binlog 订阅或设置合理过期时间。

### 缓存穿透、击穿、雪崩

| 问题 | 含义 | 解决 |
| --- | --- | --- |
| 缓存穿透 | 查不存在的数据，请求一直打到数据库 | 参数校验、缓存空值、布隆过滤器 |
| 缓存击穿 | 热点 key 失效，大量请求打到数据库 | 互斥锁、逻辑过期、热点 key 不过期 |
| 缓存雪崩 | 大量 key 同时失效或 Redis 故障 | 过期时间随机化、多级缓存、限流降级 |

### 分布式锁

Redis 分布式锁常用 `SET key value NX EX seconds` 实现。`NX` 保证不存在才设置，`EX` 设置过期时间，避免死锁。释放锁时要校验 value，防止误删别人的锁。更严格的场景要考虑锁续期、主从切换和 Redlock 的适用边界。

## 笔试常考

- Redis 五种基础数据结构。
- Redis 为什么快。
- RDB 和 AOF 区别。
- 主从、哨兵、Cluster 区别。
- 缓存穿透、击穿、雪崩。
- Redis 分布式锁怎么实现。

## 面试应对

### Redis 为什么这么快？

回答思路：从内存、单线程、IO 多路复用和数据结构回答。

回答模板：

Redis 快主要有几个原因：第一，数据主要存放在内存中，访问延迟很低；第二，命令执行主路径是单线程，避免了复杂锁竞争和线程上下文切换；第三，使用 IO 多路复用处理大量网络连接；第四，底层数据结构针对常见操作做了优化。需要补充的是，Redis 并不是所有工作都绝对单线程，持久化、删除和网络 IO 等场景可能有后台线程参与。

### 缓存和数据库如何保持一致？

回答思路：先说常见方案，再说并发风险和补偿。

回答模板：

常见方案是先更新数据库，再删除缓存。删除缓存比更新缓存更稳，因为缓存值可能由复杂逻辑计算而来，并发写入时直接更新缓存容易出现旧值覆盖新值。这个方案仍然可能有短暂不一致，所以工程上会配合过期时间、延迟双删、消息队列重试、binlog 订阅和监控补偿。核心目标不是做到绝对实时一致，而是在业务可接受范围内控制不一致窗口。

### Redis 分布式锁怎么实现？

回答思路：说明原子加锁、过期时间、唯一标识和安全释放。

回答模板：

Redis 分布式锁常用 `SET key value NX EX seconds` 实现，`NX` 保证 key 不存在时才能加锁，`EX` 设置过期时间避免死锁。value 要使用唯一标识，释放锁时先校验 value 再删除，防止误删其他客户端的锁。更严格的场景还要考虑锁续期、主从切换和业务能否接受 Redis 锁的一致性边界。
