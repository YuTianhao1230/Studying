# Workflow

## 一句话解释

Workflow 是预先设计好的多步骤执行流程，用代码或配置把 LLM、工具和规则串起来，让系统按确定路径完成任务。

## Workflow 和 Agent 的区别

Workflow 更强调“流程预定义”：

```text
步骤 A -> 步骤 B -> 步骤 C -> 输出
```

Agent 更强调“模型动态决策”：

```text
观察状态 -> 模型决定下一步 -> 执行 -> 再观察 -> 再决策
```

二者不是对立关系。很多生产系统会把 Workflow 和 Agent 混合使用：

- 外层用 Workflow 控制关键流程和安全边界。
- 局部让 Agent 处理开放问题、搜索、生成、调试或归纳。

## 常见 Workflow 模式

### 1. Prompt Chaining

多个 LLM 调用顺序连接，前一步输出作为后一步输入。

适合：

- 文档总结后再改写。
- 需求转 PRD，再转技术方案。
- 代码生成后再生成测试。

### 2. Routing

先判断任务类型，再路由到不同模型、工具或专家链路。

适合：

- 客服问题分流。
- 代码问题按语言分流。
- 多模态任务按输入类型分流。

### 3. Parallelization

把任务拆成多个子任务并行执行，再汇总结果。

适合：

- 多文档阅读。
- 多文件代码审查。
- 多候选答案投票。

### 4. Orchestrator-Workers

一个主控节点拆分任务，多个 worker 执行，最后由主控汇总。

适合：

- 大规模代码修改。
- 多模块调研。
- 复杂数据分析。

### 5. Evaluator-Optimizer

一个模型生成结果，另一个模型或规则系统评估，未达标则继续修改。

适合：

- 文案优化。
- 代码修复。
- Prompt 迭代。
- 测试用例补全。

## 为什么 Workflow 很重要

Agent 容易开放、不可控；Workflow 能提供：

- 稳定步骤。
- 可复现执行。
- 明确验收点。
- 成本和权限控制。
- 更好的失败恢复。

## 常见误区

- 以为 Agent 越自主越好。
- 把固定流程交给模型自由规划，导致成本和不确定性上升。
- 没有在关键步骤设置校验和中止条件。
- 所有步骤都用 LLM，而不是把确定性逻辑写成代码。

## 面试可能怎么问

1. Workflow 和 Agent 的区别是什么？
2. 什么时候应该用固定 Workflow，而不是自主 Agent？
3. 解释 Prompt Chaining、Routing、Parallelization。
4. 如何设计一个带质量回路的 Evaluator-Optimizer？
5. Workflow 如何降低 Agent 系统的不确定性？
