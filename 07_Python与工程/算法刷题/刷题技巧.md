# 刷题技巧

## 知识点解析

### 概述

动态规划 最大子数组和（最大子段和） leecode53题 https://leetcode.cn/problems/maximum-subarray/description/ 定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态。

### 最大子数组和（最大子段和）
leecode53题 https://leetcode.cn/problems/maximum-subarray/description/  
定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态转移方程 f[i]=max(f[i−1],0)+a[i]，答案为 max(f)。

$$
f[i] = \begin{cases} 
nums[i], & i = 0 \\
\max(f[i - 1], 0) + nums[i], & i \geq 1
\end{cases}
$$

### 代码解释：
*   `$$ ... $$`：表示这是独立的一行数学公式（块级公式）。
*   `\begin{cases} ... \end{cases}`：这是专门用来写分段函数（带左侧大括号）的环境。
*   `&`：用于对齐，通常放在条件（如 $i=0$）的前面。
*   `\\`：表示换行。
*   `\max`：表示最大值函数，会显示为正体的 "max"。
*   `\geq`：表示大于等于符号 ($\ge$)。

## 面试应对

### 动态规划 是什么？

回答思路：先给定义，再说明它解决的问题，最后点出它在 Python 与算法工程 中的定位。

回答模板：

刷题技巧 的核心含义是：动态规划 最大子数组和（最大子段和） leecode53题 https://leetcode.cn/problems/maximum-subarray/description/ 定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态。 它出现是为了解决特定任务中的效果、效率、稳定性或工程复杂度问题。机制上，最大子数组和（最大子段和） leecode53题 https://leetcode.cn/problems/maximum-subarray/description/ 定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态转移方程 f[i]=max(f[i−1],0)+a[i]，答案为 max(f)。 使用时要注意边界，它不是万能方案，必须结合数据质量、成本和实际指标判断是否值得引入。

### 动态规划 在什么场景下会用到？

回答思路：从背景痛点切入，说明什么条件下值得使用，再补充不适合的情况。

回答模板：

动态规划 适合用在它解决的问题真实存在的场景。leecode53题 https://leetcode.cn/problems/maximum-subarray/description/定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态转移方程 f[i]=max(f[i−1]。 实际项目里，我会先判断当前瓶颈是否和这个问题匹配，例如效果、效率、稳定性、数据质量或工程可维护性。如果瓶颈不在这里，或者引入 动态规划 会显著增加系统复杂度和排障成本，就不应该为了追概念而使用。

### 动态规划 的作用机制是什么？

回答思路：把机制拆成关键模块或步骤，不要只背结论；涉及工程时要讲它如何影响效果、成本或稳定性。

回答模板：

刷题技巧 的机制要从输入、处理过程和输出结果三层理解。最大子数组和（最大子段和） leecode53题 https://leetcode.cn/problems/maximum-subarray/description/ 定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态转移方程 f[i]=max(f[i−1],0)+a[i]，答案为 max(f)。 面试里我会进一步说明关键步骤如何连接，以及这些步骤为什么会影响效果、效率或稳定性；同时也要说明机制成立的前提，比如数据质量、参数配置和计算成本。

### 动态规划 有哪些优劣和限制？

回答思路：优点从效果、效率、稳定性、可维护性讲；限制从数据质量、成本、复杂度和适用边界讲。

回答模板：

动态规划 的价值要和限制一起看。优势上，动态规划 的收益需要和成本一起评估，重点关注效果、效率、稳定性、实现复杂度和适用边界。 但它也可能带来额外成本，例如数据依赖、实现复杂度、调参难度、稳定性风险或线上维护成本。真实项目里不能只看理论收益，而要通过 ablation、分桶评测、bad case 分析和成本指标确认净收益。

### 如果在项目中使用 动态规划，你会如何验证它有效？

回答思路：按工程闭环回答：目标指标、对照实验、分桶分析、bad case、成本和回归验证。

回答模板：

刷题技巧 的机制要从输入、处理过程和输出结果三层理解。最大子数组和（最大子段和） leecode53题 https://leetcode.cn/problems/maximum-subarray/description/ 定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态转移方程 f[i]=max(f[i−1],0)+a[i]，答案为 max(f)。 面试里我会进一步说明关键步骤如何连接，以及这些步骤为什么会影响效果、效率或稳定性；同时也要说明机制成立的前提，比如数据质量、参数配置和计算成本。
