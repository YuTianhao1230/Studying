# 量化

## 知识点解析

### 概述

量化是把模型中的高精度数值表示转换成低精度表示，从而降低显存占用、减少计算成本、提升推理速度。

### 为什么需要量化

大模型参数量很大。

如果使用 FP16：

```text
1 个参数约 2 字节
7B 参数约 14GB
70B 参数约 140GB
```

如果使用 INT4：

```text
1 个参数约 0.5 字节
显存占用可以大幅降低
```

量化的直接收益：

- 模型能放进更少 GPU。
- 推理吞吐可能提升。
- 部署成本下降。

### 常见精度格式

- FP32：32 位浮点，精度高，成本高。
- FP16：16 位浮点，训练和推理常用。
- BF16：16 位浮点，动态范围更大，训练更稳定。
- FP8：8 位浮点，硬件支持要求较高。
- INT8：8 位整数，常见推理量化。
- INT4：4 位整数，显存更省，但精度风险更大。

### 权重量化和激活量化

#### 权重量化

只量化模型权重。

优点：

- 实现相对简单。
- 显存收益明显。

缺点：

- 计算中激活仍可能较大。

#### 激活量化

量化中间激活值。

优点：

- 能进一步降低计算和显存成本。

缺点：

- 对数值误差更敏感。
- 校准更复杂。

### PTQ 和 QAT

#### PTQ，Post-Training Quantization

训练后量化，不重新训练或只做少量校准。

优点：

- 成本低。
- 部署快。

缺点：

- 精度可能下降。

#### QAT，Quantization-Aware Training

训练时模拟量化误差，让模型适应低精度。

优点：

- 精度通常更好。

缺点：

- 训练成本高。
- 流程复杂。

### 量化的风险

- 数学计算能力下降。
- 长文本稳定性变差。
- 结构化输出更容易出错。
- 小模型比大模型更容易受影响。
- 特定领域能力可能退化。

### 如何评估量化是否可用

不能只看平均分，要看：

- 总体指标。
- 关键任务指标。
- 长尾任务。
- 格式正确率。
- 数学/代码/推理能力。
- 延迟和吞吐。
- 显存节省。
- bad case 差异。

## 面试应对

### 量化是什么，为什么能省显存？

回答思路：先定义问题，再说明核心机制、适用边界、风险和验证方式。

回答模板：

量化 的优点要结合它解决的问题来看：通常是提升效果、降低成本、增强稳定性或改善工程可维护性。限制则主要来自数据质量、计算资源、实现复杂度和失败模式。真实项目里我会用消融实验、分桶评测、bad case 分析和成本指标判断它是否真的有净收益。

### INT8 和 INT4 的区别是什么？

回答思路：先分别定义两个概念，再从目标、机制、适用场景和工程取舍四个角度对比。

回答模板：

INT8 和 INT4 的区别 的区别要看目标和控制方式。INT8 更偏一种机制或能力，INT4 的区别 更偏另一类抽象或系统边界。在 量化 场景里，二者通常不是互斥关系，而是组合使用：稳定部分交给确定流程，开放或动态部分交给更灵活的机制。

### PTQ 和 QAT 有什么区别？

回答思路：先分别定义两个概念，再从目标、机制、适用场景和工程取舍四个角度对比。

回答模板：

我会先分别定义相关概念，再从目标、输入数据、核心机制、工程成本和适用场景对比。围绕 量化，关键是说明它解决的是哪类问题，以及相比相近方案的取舍：有的方案更简单稳定，有的方案探索能力更强但成本更高。最后要落到工程选择标准，而不是只列名词。

### 量化后模型效果下降怎么办？

回答思路：先定义问题，再说明核心机制、适用边界、风险和验证方式。

回答模板：

量化 的机制需要按输入、处理过程、输出结果和约束条件来讲。核心不是背概念，而是说明它如何改变模型或系统行为，为什么能带来收益，以及在什么条件下会失效。结合这篇笔记，关键起点是：量化是把模型中的高精度数值表示转换成低精度表示，从而降低显存占用、减少计算成本、提升推理速度。

### 如何评估一个量化模型是否能上线？

回答思路：按“目标定义 -> 关键步骤 -> 风险控制 -> 指标验证”的顺序回答，避免只讲抽象原则。

回答模板：

我会把 评估一个量化模型是否能上线 拆成几个步骤：先明确目标和输入输出，再设计关键模块或策略，然后加入失败处理和权限/质量约束，最后用离线指标、线上指标或回归测试验证效果。在 量化 场景里，重点是能落到真实工程流程。
