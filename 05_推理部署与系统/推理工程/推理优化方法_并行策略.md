# 推理优化方法_并行策略

## 知识点解析

### 概述

推理优化是为了在保证效果的前提下，让模型推理更快、更省显存、更稳定；并行策略是把模型、数据或计算拆到多张 GPU / 多台机器上执行。

### 推理优化主要优化什么

核心目标通常有三个：

- 降低延迟：用户更快拿到结果。
- 提升吞吐：单位时间处理更多请求或 token。
- 降低成本：使用更少 GPU、显存和机器资源。

常见指标：

- TTFT：首 token 延迟。
- TPOT：每个输出 token 的生成时间。
- QPS：每秒请求数。
- Throughput：每秒处理 token 数。
- GPU 利用率。
- 显存占用。
- 错误率和超时率。

### 大模型推理的两个阶段

```text
输入 prompt
  -> Prefill：一次性处理输入上下文
  -> Decode：逐 token 生成输出
```

Prefill：

- 处理完整输入。
- 计算量大，但并行度较高。
- 输入越长，prefill 越慢。

Decode：

- 每次生成一个 token。
- 强依赖 KV cache。
- 容易受访存和调度影响。

### 常见推理优化方法

#### Batching

把多个请求合并成一个 batch，提高 GPU 利用率。

问题：

- 请求长度不同会造成 padding 浪费。
- 长请求可能拖慢短请求。

优化：

- dynamic batching。
- continuous batching。
- 按长度分桶。

#### KV Cache

缓存历史 token 的 Key 和 Value，避免 decode 时重复计算。

收益：

- 大幅降低自回归生成的重复计算。

代价：

- 占用大量显存。
- 长上下文和大 batch 会让 KV cache 爆显存。

#### FlashAttention

优化 Attention 的计算和显存访问。

核心价值：

- 减少中间 attention matrix 的显存占用。
- 提升 GPU 访存效率。

#### 算子融合

把多个小算子合并执行。

收益：

- 减少 kernel launch。
- 减少中间 tensor 读写。
- 提升吞吐。

#### 量化

把权重或激活从 FP16/BF16 压缩到 INT8、INT4、FP8 等。

收益：

- 降低显存。
- 提升吞吐。

风险：

- 精度下降。
- 长尾能力受损。
- 某些任务对数值误差敏感。

#### Speculative Decoding

用小模型先草拟多个 token，再由大模型验证。

适合：

- 大模型 decode 成本高。
- 小模型和大模型输出分布接近。

#### Prompt / Context 优化

减少输入 token 数也能直接优化推理成本。

方法：

- 压缩 prompt。
- 删除无用上下文。
- RAG 只放高相关片段。
- 控制 max_tokens。

### 并行策略

#### Data Parallelism，数据并行

多张 GPU 各自处理不同请求或不同 batch。

```text
GPU0: request batch A
GPU1: request batch B
GPU2: request batch C
```

特点：

- 模型在每张卡上都有完整副本。
- 实现简单。
- 适合模型能完整放进单卡的场景。

限制：

- 如果单卡放不下模型，就不能只靠数据并行。

#### Tensor Parallelism，张量并行

把单层里的大矩阵计算拆到多张 GPU。

例如一个 Linear 层权重很大，可以按列或按行切分：

```text
W = [W0, W1, W2, W3]
GPU0 算 W0
GPU1 算 W1
GPU2 算 W2
GPU3 算 W3
```

特点：

- 适合单卡放不下模型或单层计算太大的场景。
- 每层都可能需要通信。

限制：

- 通信开销高。
- 对网络带宽和拓扑敏感。

#### Pipeline Parallelism，流水线并行

把模型不同层放在不同 GPU 上。

```text
GPU0: Layer 0-7
GPU1: Layer 8-15
GPU2: Layer 16-23
GPU3: Layer 24-31
```

特点：

- 适合层数很多、模型很大的场景。
- 每个 micro-batch 像流水线一样经过不同 GPU。

限制：

- 有 pipeline bubble，GPU 可能等待。
- 推理场景下 decode 逐 token 生成，流水线调度更复杂。

#### Sequence Parallelism，序列并行

把序列长度维度拆到不同 GPU 上。

适合：

- 长上下文训练或推理。
- attention 或 norm 在序列维度上占用较大。

限制：

- 实现复杂。
- 通信模式要求高。

#### Expert Parallelism，专家并行

主要用于 MoE 模型，把不同专家放到不同 GPU。

```text
Router
  -> Expert 1 on GPU0
  -> Expert 2 on GPU1
  -> Expert 3 on GPU2
```

特点：

- 每个 token 只激活部分专家。
- 可以扩大模型总参数量，同时控制每次计算量。

限制：

- token 分发会产生 all-to-all 通信。
- 负载不均衡会影响吞吐。

### 训练并行和推理并行的差异

训练并行：

- 需要前向、反向、梯度同步。
- 还要处理优化器状态和激活保存。

推理并行：

- 只做前向。
- 更关注 KV cache、请求调度、decode 延迟和服务稳定性。

## 面试应对

### 数据并行、张量并行、流水线并行有什么区别？

回答思路：先分别定义两个概念，再从目标、机制、适用场景和工程取舍四个角度对比。

回答模板：

我会先分别定义相关概念，再从目标、输入数据、核心机制、工程成本和适用场景对比。围绕 推理优化方法_并行策略，关键是说明它解决的是哪类问题，以及相比相近方案的取舍：有的方案更简单稳定，有的方案探索能力更强但成本更高。最后要落到工程选择标准，而不是只列名词。

### 模型单卡放不下时怎么办？

回答思路：先定义问题，再说明核心机制、适用边界、风险和验证方式。

回答模板：

推理优化方法_并行策略 的机制需要按输入、处理过程、输出结果和约束条件来讲。核心不是背概念，而是说明它如何改变模型或系统行为，为什么能带来收益，以及在什么条件下会失效。结合这篇笔记，关键起点是：推理优化是为了在保证效果的前提下，让模型推理更快、更省显存、更稳定；并行策略是把模型、数据或计算拆到多张 GPU / 多台机器上执行。

### 为什么 tensor parallel 通信开销大？

回答思路：先指出背后的核心约束，再解释收益，最后补充如果不这样做会带来的风险。

回答模板：

因为 tensor parallel 通信开销大 背后有明确工程约束：如果不处理，容易带来质量不稳定、成本上升、错误难排查或安全风险。在 推理优化方法 并行策略 场景里，这个问题的关键是说明它如何提升系统可控性、可验证性和线上稳定性。

### 大模型推理为什么 decode 阶段容易成为瓶颈？

回答思路：先定义问题，再说明核心机制、适用边界、风险和验证方式。

回答模板：

推理优化方法_并行策略 的核心含义是：推理优化是为了在保证效果的前提下，让模型推理更快、更省显存、更稳定；并行策略是把模型、数据或计算拆到多张 GPU / 多台机器上执行。 面试回答时我会先给定义，再说明它解决的背景问题，接着讲核心机制和适用场景，最后补充限制与工程风险。这样回答比只背一句概念更完整。

### 如何优化首 token 延迟？

回答思路：按“目标定义 -> 关键步骤 -> 风险控制 -> 指标验证”的顺序回答，避免只讲抽象原则。

回答模板：

我会把 优化首 token 延迟 拆成几个步骤：先明确目标和输入输出，再设计关键模块或策略，然后加入失败处理和权限/质量约束，最后用离线指标、线上指标或回归测试验证效果。在 推理优化方法 并行策略 场景里，重点是能落到真实工程流程。

### 如何优化吞吐？

回答思路：按“目标定义 -> 关键步骤 -> 风险控制 -> 指标验证”的顺序回答，避免只讲抽象原则。

回答模板：

我会把 优化吞吐 拆成几个步骤：先明确目标和输入输出，再设计关键模块或策略，然后加入失败处理和权限/质量约束，最后用离线指标、线上指标或回归测试验证效果。在 推理优化方法 并行策略 场景里，重点是能落到真实工程流程。

### 长上下文推理为什么显存压力大？

回答思路：先定义问题，再说明核心机制、适用边界、风险和验证方式。

回答模板：

推理优化方法_并行策略 的核心含义是：推理优化是为了在保证效果的前提下，让模型推理更快、更省显存、更稳定；并行策略是把模型、数据或计算拆到多张 GPU / 多台机器上执行。 面试回答时我会先给定义，再说明它解决的背景问题，接着讲核心机制和适用场景，最后补充限制与工程风险。这样回答比只背一句概念更完整。

#### 回答要点

```text
推理优化要先区分 prefill 和 decode。
Prefill 更偏大矩阵并行计算，decode 更受 KV cache、访存和调度影响。
如果模型单卡能放下，可以优先数据并行和 batching；如果单卡放不下，需要 tensor parallel 或 pipeline parallel。
同时还可以通过 KV cache 管理、FlashAttention、量化、算子融合、continuous batching 等方式提升吞吐和降低显存。
```
